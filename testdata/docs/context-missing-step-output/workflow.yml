name: ci-context-missing-step-output
'on': push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - id: prepare
        shell: bash
        run: echo prepare
      - id: consume
        shell: bash
        run: printf '<%s>' "${{ steps.prepare.outputs.missing }}"
