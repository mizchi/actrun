name: e2e-checkout-container
'on': push
jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: node:20-bookworm
    steps:
      - id: checkout
        uses: actions/checkout@v4
      - id: verify
        shell: bash
        run: |
          mkdir -p artifacts
          printf '%s|%s' "$(cat tracked.txt)" "$(test -f .git/shallow && echo yes || echo no)" > artifacts/result.txt
