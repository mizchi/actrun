name: e2e-remote-nested-cache-hit
'on': push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: demo/tools/composite@v1
      - id: verify
        run: |
          mkdir -p artifacts
          cat remote.txt > artifacts/remote.txt
