name: e2e-repo-head-detect
'on':
  push:
    paths:
      - src/**
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - id: copy
        run: |
          mkdir -p artifacts
          cat src/message.txt > artifacts/message.txt
      - id: context
        run: |
          printf '%s|%s|%s|%s' "${GITHUB_ACTOR}" "${GITHUB_REPOSITORY}" "${GITHUB_REPOSITORY_OWNER}" "${GITHUB_SHA}" > artifacts/context.txt
