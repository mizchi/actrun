name: e2e-docker-failure-cleanup
'on': push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - id: remote
        uses: demo/tools/action@v1
      - id: verify
        shell: bash
        run: |
          mkdir -p artifacts
          cat main.txt > artifacts/main.txt
