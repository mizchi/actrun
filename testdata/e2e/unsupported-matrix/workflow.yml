name: e2e-unsupported-matrix
'on': push
jobs:
  build:
    strategy:
      matrix:
        os:
          - ubuntu-latest
        include:
          - os: ubuntu-latest
            target: native
    runs-on: ubuntu-latest
    steps:
      - run: printf '%s' 'nope'
