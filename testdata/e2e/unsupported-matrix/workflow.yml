name: e2e-unsupported-matrix
'on': push
jobs:
  build:
    strategy:
      max-parallel: 0
      matrix:
        os:
          - ubuntu-latest
    runs-on: ubuntu-latest
    steps:
      - id: emit
        run: printf '%s' ok
