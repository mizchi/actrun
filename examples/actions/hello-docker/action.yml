name: Hello Docker
description: A simple Docker action example
inputs:
  name:
    description: Who to greet
    required: true
    default: World
runs:
  using: docker
  image: Dockerfile
  args:
    - ${{ inputs.name }}
